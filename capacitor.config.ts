import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jhs.test.exam',
  appName: 'exam1',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
